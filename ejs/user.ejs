<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>ejs home</title>
	<link href="../lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
	<style type="text/css">
		.liked_movie{
			display: inline-block;
			border: 1px solid #ccc;
			padding: 5px;
			background: #abc;
			text-transform: capitalize;
		}
		ul{
			margin: 0;
			padding: 0;
		}
		li{
			list-style-type: none;
		}
	</style>
</head>
<body>
		<% include header %>
	<div class="container">
		<div class="row">
			<div class="col-md-10">
				<h2>User List</h2>

					
				<% if ( user ) { %>
					<ul>
						<% for(var u of user) { %>
							<li>
								<div class="panel panel-default">
									<div class="panel-heading"><a href="/user/<%= u._id %>"><%= u.name %></a></div>
									<div class="panel-body">
										<div class="col-xs-3 col-sm-3 col-md-3 col-lg-3">
											<a href="user/<%= u._id %>" class="thumbnail">
												<img src=" <%= u.avater %>" alt="<%= u.name %>">
											</a>
										</div>
										<p><%= u.name %></p>
										<p>Age : <%= u.age %></p>
										<p><%= u.position %></p>
										
										<p><button rel="<%= u._id %>" class="edit"> Follow </button></p>
									</div>
								</div>
							</li>
						<% } %>
					</ul>
				<% } %>
			</div>
		</div>
	</div>
	
	<div class="container">
		<div class="row">
			<div class="col-md-12">
				
			</div>
		</div>
	</div>
	<script type="text/javascript" src= "../lib/jquery/dist/jquery.min.js" ></script>
	<script type="text/javascript" src= "../js/script.js" ></script>
</body>
</html>